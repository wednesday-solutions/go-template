// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testUserSubjects(t *testing.T) {
	t.Parallel()

	query := UserSubjects()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testUserSubjectsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserSubjects().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserSubjectsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := UserSubjects().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserSubjects().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserSubjectsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserSubjectSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserSubjects().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserSubjectsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := UserSubjectExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if UserSubject exists: %s", err)
	}
	if !e {
		t.Errorf("Expected UserSubjectExists to return true, but got false.")
	}
}

func testUserSubjectsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	userSubjectFound, err := FindUserSubject(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if userSubjectFound == nil {
		t.Error("want a record, got nil")
	}
}

func testUserSubjectsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = UserSubjects().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testUserSubjectsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := UserSubjects().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testUserSubjectsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	userSubjectOne := &UserSubject{}
	userSubjectTwo := &UserSubject{}
	if err = randomize.Struct(seed, userSubjectOne, userSubjectDBTypes, false, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}
	if err = randomize.Struct(seed, userSubjectTwo, userSubjectDBTypes, false, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userSubjectOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userSubjectTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserSubjects().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testUserSubjectsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	userSubjectOne := &UserSubject{}
	userSubjectTwo := &UserSubject{}
	if err = randomize.Struct(seed, userSubjectOne, userSubjectDBTypes, false, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}
	if err = randomize.Struct(seed, userSubjectTwo, userSubjectDBTypes, false, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userSubjectOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userSubjectTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserSubjects().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func userSubjectBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserSubject) error {
	*o = UserSubject{}
	return nil
}

func userSubjectAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserSubject) error {
	*o = UserSubject{}
	return nil
}

func userSubjectAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *UserSubject) error {
	*o = UserSubject{}
	return nil
}

func userSubjectBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserSubject) error {
	*o = UserSubject{}
	return nil
}

func userSubjectAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserSubject) error {
	*o = UserSubject{}
	return nil
}

func userSubjectBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserSubject) error {
	*o = UserSubject{}
	return nil
}

func userSubjectAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserSubject) error {
	*o = UserSubject{}
	return nil
}

func userSubjectBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserSubject) error {
	*o = UserSubject{}
	return nil
}

func userSubjectAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserSubject) error {
	*o = UserSubject{}
	return nil
}

func testUserSubjectsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &UserSubject{}
	o := &UserSubject{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, userSubjectDBTypes, false); err != nil {
		t.Errorf("Unable to randomize UserSubject object: %s", err)
	}

	AddUserSubjectHook(boil.BeforeInsertHook, userSubjectBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	userSubjectBeforeInsertHooks = []UserSubjectHook{}

	AddUserSubjectHook(boil.AfterInsertHook, userSubjectAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	userSubjectAfterInsertHooks = []UserSubjectHook{}

	AddUserSubjectHook(boil.AfterSelectHook, userSubjectAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	userSubjectAfterSelectHooks = []UserSubjectHook{}

	AddUserSubjectHook(boil.BeforeUpdateHook, userSubjectBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	userSubjectBeforeUpdateHooks = []UserSubjectHook{}

	AddUserSubjectHook(boil.AfterUpdateHook, userSubjectAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	userSubjectAfterUpdateHooks = []UserSubjectHook{}

	AddUserSubjectHook(boil.BeforeDeleteHook, userSubjectBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	userSubjectBeforeDeleteHooks = []UserSubjectHook{}

	AddUserSubjectHook(boil.AfterDeleteHook, userSubjectAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	userSubjectAfterDeleteHooks = []UserSubjectHook{}

	AddUserSubjectHook(boil.BeforeUpsertHook, userSubjectBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	userSubjectBeforeUpsertHooks = []UserSubjectHook{}

	AddUserSubjectHook(boil.AfterUpsertHook, userSubjectAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	userSubjectAfterUpsertHooks = []UserSubjectHook{}
}

func testUserSubjectsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserSubjects().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserSubjectsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(userSubjectColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := UserSubjects().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserSubjectToOneSubjectUsingSubject(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local UserSubject
	var foreign Subject

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userSubjectDBTypes, false, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, subjectDBTypes, false, subjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Subject struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.SubjectID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Subject().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := UserSubjectSlice{&local}
	if err = local.L.LoadSubject(ctx, tx, false, (*[]*UserSubject)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Subject == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Subject = nil
	if err = local.L.LoadSubject(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Subject == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testUserSubjectToOneUserUsingUser(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local UserSubject
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userSubjectDBTypes, false, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.UserID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := UserSubjectSlice{&local}
	if err = local.L.LoadUser(ctx, tx, false, (*[]*UserSubject)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testUserSubjectToOneSetOpSubjectUsingSubject(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserSubject
	var b, c Subject

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userSubjectDBTypes, false, strmangle.SetComplement(userSubjectPrimaryKeyColumns, userSubjectColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, subjectDBTypes, false, strmangle.SetComplement(subjectPrimaryKeyColumns, subjectColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, subjectDBTypes, false, strmangle.SetComplement(subjectPrimaryKeyColumns, subjectColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Subject{&b, &c} {
		err = a.SetSubject(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Subject != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.UserSubjects[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.SubjectID != x.ID {
			t.Error("foreign key was wrong value", a.SubjectID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.SubjectID))
		reflect.Indirect(reflect.ValueOf(&a.SubjectID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.SubjectID != x.ID {
			t.Error("foreign key was wrong value", a.SubjectID, x.ID)
		}
	}
}
func testUserSubjectToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserSubject
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userSubjectDBTypes, false, strmangle.SetComplement(userSubjectPrimaryKeyColumns, userSubjectColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.UserSubjects[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.UserID))
		reflect.Indirect(reflect.ValueOf(&a.UserID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID, x.ID)
		}
	}
}

func testUserSubjectsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserSubjectsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserSubjectSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserSubjectsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserSubjects().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	userSubjectDBTypes = map[string]string{`ID`: `integer`, `SubjectID`: `integer`, `UserID`: `integer`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `DeletedAt`: `timestamp with time zone`}
	_                  = bytes.MinRead
)

func testUserSubjectsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(userSubjectPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(userSubjectAllColumns) == len(userSubjectPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserSubjects().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testUserSubjectsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(userSubjectAllColumns) == len(userSubjectPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserSubject{}
	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserSubjects().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userSubjectDBTypes, true, userSubjectPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(userSubjectAllColumns, userSubjectPrimaryKeyColumns) {
		fields = userSubjectAllColumns
	} else {
		fields = strmangle.SetComplement(
			userSubjectAllColumns,
			userSubjectPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := UserSubjectSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testUserSubjectsUpsert(t *testing.T) {
	t.Parallel()

	if len(userSubjectAllColumns) == len(userSubjectPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := UserSubject{}
	if err = randomize.Struct(seed, &o, userSubjectDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserSubject: %s", err)
	}

	count, err := UserSubjects().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, userSubjectDBTypes, false, userSubjectPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserSubject struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserSubject: %s", err)
	}

	count, err = UserSubjects().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
