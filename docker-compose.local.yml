version: '3.8'

services:
  server:
    stdin_open: true
    tty: true
    build:
      target: local
    env_file:
      - .env.local
    volumes:
      - type: bind
        source: .
        target: /go/src/server

